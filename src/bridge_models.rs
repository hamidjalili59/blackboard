// این ساختار یک نقطه (x, y) را نمایش می‌دهد و برای ارسال مختصات
// از فلاتر به Rust استفاده می‌شود.
pub struct Point {
    pub dx: f64,
    pub dy: f64,
}

// این ساختار به عنوان یک "کانتینر" برای ارسال رویدادهای باینری (Protobuf)
// از Rust به فلاتر از طریق استریم استفاده می‌شود.
pub struct EventMessage {
    pub data: Vec<u8>,
}
